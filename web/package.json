{
  "name": "web",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --host ${HOST:-127.0.0.1} --port ${PORT:-3000}",
    "build": "vue-tsc --noEmit && vite build",
    "serve": "vite preview --host ${HOST:-127.0.0.1} --port ${PORT:-3000}"
  },
  "dependencies": {
    "vue": "^3.1.5",
    "vue-router": "^4.0.10"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^1.3.0",
    "@vue/compiler-sfc": "^3.1.5",
    "typescript": "^4.3.5",
    "vite": "^2.4.4",
    "vue-tsc": "^0.2.2"
  }
}